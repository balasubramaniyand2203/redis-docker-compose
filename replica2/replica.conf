# Redis Stack replica configuration
replicaof redis-master 6379
masterauth ${REDIS_PASSWORD}
requirepass ${REDIS_PASSWORD}
protected-mode yes
port 6379
bind 0.0.0.0

# Enable persistence
dir /data
appendonly yes
appendfsync everysec

